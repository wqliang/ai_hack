需求：
    提升单元测试分支覆盖率到50%以上。

要求：
1.先建立单测试覆盖率基线，以此为基础，分多轮完成单元测试覆盖率提升，每轮提升10%以上,最终提升至50%以上。
2.针对src/main下所有java类。
3.单元测试类名统一以UnitTest结尾。
4.为每一个单元测试类设计场景包括：正常场景，异常场景，边界值场景。每个场景可以新建一个单元测试类。
5.使用no-daemon方式执行.\graldew.bat test --no-daemon。
6.忽略dto类，po类，vo类对象，忽略接口类，不用做单元测试。
7.引入jacoco统计单元测试覆盖率。
8.引入Mock技术避免外部依赖。
9.引入PiTest提升单元测试覆盖率。
